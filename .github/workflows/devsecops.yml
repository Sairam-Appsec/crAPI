name: crAPI-DevSecOps-Pipeline

on:
  push:
    branches: [ develop ]
  workflow_dispatch:

jobs:
  Security-Scans:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # 1. Secrets Scan (TruffleHog)
      - name: Secret Scan
        uses: trufflesecurity/trufflehog@main
        continue-on-error: true
        with:
          path: ./
          base: "" 

      # 2. SAST (SonarCloud) 
      - name: SonarCloud Scan
        uses: SonarSource/sonarcloud-github-action@master
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        with:
          args: >
            -Dsonar.projectKey=Sairam-Appsec_crAPI
            -Dsonar.organization=sairam-appsec
            -Dsonar.java.binaries=.
            -Dsonar.sources=.
            -Dsonar.c.file.suffixes=-
            -Dsonar.cpp.file.suffixes=-
            -Dsonar.objc.file.suffixes=-

      # 3. SCA (Snyk)
      - name: Snyk Security Scan
        uses: snyk/actions/node@master
        continue-on-error: true
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}

      # 4. IaC Scan (Checkov)
      - name: Checkov Scan
        uses: bridgecrewio/checkov-action@master
        with:
          directory: .
          framework: dockerfile
          soft_fail: true

      # 5. DOCKER BUILD 
      - name: Build Web Image
        run: |
          docker build -t crapi/web:latest -f services/web/Dockerfile services/web/

      # 6. Container Scan (Trivy) 
      - name: Trivy Scan
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: 'crapi/web:latest'
          format: 'table'
          exit-code: '0'
          severity: 'CRITICAL,HIGH'
          # --- PHASE 2: DYNAMIC SECURITY TESTING (DAST) ---

      # 6. Spin up crAPI
      - name: Spin up crAPI
        run: |
          cd deploy/docker
          docker compose up -d
          sleep 120 

      # 7. OWASP ZAP (Baseline Web Scan)
      - name: OWASP ZAP Baseline Scan
        run: |
          mkdir -p zap_report
          chmod 777 zap_report
          docker run --rm -v $(pwd)/zap_report:/zap/wrk/:rw --network="host" ghcr.io/zaproxy/zaproxy:stable zap-baseline.py -t http://localhost:8888 -r report.html
        continue-on-error: true

      # 8. Schemathesis (API Scan)
      - name: Schemathesis API Scan
        run: |
          docker run --network="host" schemathesis/schemathesis:latest \
          run http://localhost:8888/openapi.json \
          --checks all --base-url http://localhost:8888 > schemathesis_report.json || true
        continue-on-error: true

      # 9. Nikto (Server scan using official 'sullo/nikto' image)
      - name: Nikto Server Scan
        run: |
          docker run --rm --network="host" sullo/nikto -h http://localhost:8888 -o nikto_report.xml
        continue-on-error: true

      # 10. Stop crAPI (Cleanup)
      - name: Shutdown crAPI
        run: |
          cd deploy/docker
          docker compose down

      # --- FINAL PHASE: REPORTING ---
      - name: Upload Security Reports
        uses: actions/upload-artifact@v4
        with:
          name: Full-Security-Bundle
          path: |
            report_html.html
            schemathesis_report.json
            nikto_report.xml
            zap_report/