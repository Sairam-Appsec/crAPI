name: crAPI-DevSecOps-Pipeline

on:
  push:
    branches: [ develop ]
  workflow_dispatch:

jobs:
  Security-Scans:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # 0. Ephemeral DefectDojo Setup
      - name: Spin up Ephemeral DefectDojo
        run: |
          git clone https://github.com/DefectDojo/django-DefectDojo
          cd django-DefectDojo
          
          # GitHub Runners lo 'docker compose' (no hyphen) vaadaali
          docker compose -f docker-compose.yml up -d
          
          echo "Waiting for DefectDojo services to fully initialize..."
          # Dojo ki min 4 mins ivvali mama, database setup avvadaniki
          sleep 240
          
          # Verify chesthunnam
          docker ps

      # 1. Secrets Scan (TruffleHog)
      - name: Secret Scan
        uses: trufflesecurity/trufflehog@main
        continue-on-error: true
        with:
          path: ./
          base: "" 

      # 2. SAST (SonarCloud) 
      - name: SonarCloud Scan
        uses: SonarSource/sonarcloud-github-action@master
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        with:
          args: >
            -Dsonar.projectKey=Sairam-Appsec_crAPI
            -Dsonar.organization=sairam-appsec
            -Dsonar.java.binaries=.
            -Dsonar.sources=.
            -Dsonar.c.file.suffixes=-
            -Dsonar.cpp.file.suffixes=-
            -Dsonar.objc.file.suffixes=-

      # 3. SCA (Snyk)
      - name: Snyk Security Scan
        uses: snyk/actions/node@master
        continue-on-error: true
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}

      # 4. IaC Scan (Checkov)
      - name: Checkov Scan
        uses: bridgecrewio/checkov-action@master
        with:
          directory: .
          framework: dockerfile
          soft_fail: true

      # 5. DOCKER BUILD 
      - name: Build Web Image
        run: |
          docker build -t crapi/web:latest -f services/web/Dockerfile services/web/

      # 6. Container Scan (Trivy) 
      - name: Trivy Scan
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: 'crapi/web:latest'
          format: 'table'
          exit-code: '0'
          severity: 'CRITICAL,HIGH'
          # --- PHASE 2: DYNAMIC SECURITY TESTING (DAST) ---

      # 6. Spin up crAPI
      - name: Spin up crAPI
        run: |
          cd deploy/docker
          docker compose up -d
          sleep 120 

      # 7. OWASP ZAP (Fixing Permissions with a shell script)
      - name: OWASP ZAP Baseline Scan
        run: |
          # Creating workspace and giving full permissions to avoid 'Permission Denied'
          mkdir -p zap_out
          chmod 777 zap_out
          docker run --rm -v $(pwd)/zap_out:/zap/wrk/:rw --network="host" \
          ghcr.io/zaproxy/zaproxy:stable zap-baseline.py \
          -t http://localhost:8888 -r zap_report.html || true
          # Moving report to root so upload-artifact can find it easily
          cp zap_out/zap_report.html ./report_html.html || true

      # 8. Schemathesis (Direct check for content)
      - name: Schemathesis API Scan
        run: |
          pip install schemathesis
          echo "Waiting for crAPI services..."
          timeout 180s bash -c 'until curl -s http://localhost:8888/openapi.json; do sleep 5; done'
          schemathesis run http://localhost:8888/openapi.json --checks all > schemathesis_report.json || true

      # 9. Nikto (Native install to avoid docker permission issues)
      - name: Nikto Server Scan
        run: |
          sudo apt-get update && sudo apt-get install -y nikto
          nikto -h http://localhost:8888 -o nikto_report.xml -Format xml || true

      # --- PHASE 3: REPORTING & INTEGRATION ---

      # 11. Auto-Push to DefectDojo
      - name: Auto-Push to DefectDojo
        if: always()
        run: |
          echo "Locating and pushing results..."
          
          # ZAP Report ethiki push chesthunnam
          ZAP_FILE=$(find . -name "report_html.html" | head -n 1)
          if [ -n "$ZAP_FILE" ]; then
            curl -X POST "http://localhost:8080/api/v2/import-scan/" \
            -H "Authorization: Token ${{ secrets.DOJO_TOKEN }}" \
            -F "active=true" \
            -F "verified=true" \
            -F "scan_type=ZAP Scan" \
            -F "product_name=crAPI" \
            -F "engagement_name=Automated-Scan" \
            -F "file=@$ZAP_FILE"
            echo "ZAP Report Sent!"
          else
            echo "ZAP Report not found anywhere!"
          fi

      - name: Upload Security Reports Artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: Full-Security-Bundle
          path: |
            report_html.html
            schemathesis_report.json
            nikto_report.xml

      # 11. Shutdown crAPI
      - name: Shutdown crAPI
        if: always()
        run: |
          cd deploy/docker
          docker compose down || true